import Placeholder from '../components/Placeholder';

export default function PipelinePage() {
  return (
    <Placeholder
      title="PipeDeal"
      note="Kanban deal pipeline (Buyer Matching → LOI/NDA → Due Diligence → Negotiation → Docs Signed → Funded) — delivered in Phase 7."
    />
  );
}
