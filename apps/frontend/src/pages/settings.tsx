import Placeholder from '../components/Placeholder';

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      note="Outreach toggle, lead parameters, data export/import, help videos, and theme — wired with onboarding and billing in a later phase."
    />
  );
}
