import Placeholder from '../components/Placeholder';

export default function VaultPage() {
  return (
    <Placeholder
      title="Document Vault"
      note="Branded NDA / LOI / representation templates and outreach PDFs — wired with the document service in a later phase."
    />
  );
}
